import { TopSection, ModalContainer } from './Styles';

const CreateSubforumModal = ({ closeModal }) => {
  return (
    <TopSection>
      <ModalContainer></ModalContainer>
    </TopSection>
  );
};

export default CreateSubforumModal;
